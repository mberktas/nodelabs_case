import 'package:flutter/widgets.dart';

class DSError extends StatelessWidget {
  const DSError({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('An error occurred. Please try again later.'));
  }
}
